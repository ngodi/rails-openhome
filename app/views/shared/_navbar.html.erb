<nav class="navbar">
      <div class="container">
        <div class="navbar-header">
          <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <%= link_to "OpenHomes", root_path, class: "navbar-brand" %>
        </div>
        
        <div id="navbar" class="navbar-collapse collapse">
          <ul class="nav navbar-nav navbar-right">
              <% if signed_in? %>
            <li class="dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
               <%= image_tag avatar_url(current_user), class: "image-small", width: "30px" %>
                <%= current_user.fullname.split[0] %>
              <span class="caret"></span></a>
              <ul class="dropdown-menu">
                <li><%= link_to "Your Listings", rooms_path %></li>
                <li><%= link_to "Your Reservations", your_reservations_path %></li>
                <li><%= link_to "Your Trips", your_trips_path %></li>
                <li><%= link_to "Wishlist", ""%></li>                
                <li><%= link_to "Edit Profile", edit_user_registration_path(current_user.id)%></li>
                <li role="separator" class="divider"></li>
                <li><%= link_to "Account Setting", user_path(current_user.id)%></li>
                <li><%= link_to "Logout", destroy_user_session_path, method: :delete %></li>
              </ul>
            </li>
            <% else %>
              <li>
                <%= link_to "Login", new_user_session_path %>
              </li>
            <% end %>
            <li>
              <%= link_to conversations_path do %>
                   <i class="fa fa-envelope"></i> Messages
                <% end %>
            </li>
             <li>
              <%= link_to new_room_path do %>
                   <i class="fas fa-home"></i>Become a Host
                <% end %>
            </li>
          </ul>
        </div><!--/.nav-collapse -->
      </div>
    </nav>